--- origsrc/dee-1.0.10/vapi/Makefile.am	2012-01-02 06:32:42.000000000 -0600
+++ src/dee-1.0.10/vapi/Makefile.am	2012-05-14 01:29:10.356637700 -0500
@@ -6,7 +6,7 @@ if HAVE_INTROSPECTION
 if HAVE_VAPIGEN
 
 dee-1.0.vapi: $(top_builddir)/src/Dee-$(DEE_MAJOR_VERSION).$(DEE_MINOR_VERSION).gir $(srcdir)/Dee-1.0-custom.vala $(srcdir)/Dee-1.0.metadata
-	$(AM_V_GEN)$(VALA_API_GEN) --library dee-1.0 --metadatadir=$(srcdir) $(filter %.gir %.vala,$^)
+	$(AM_V_GEN)$(VALA_API_GEN) --library dee-1.0 --pkg gio-2.0 --metadatadir=$(srcdir) $(filter %.gir %.vala,$^)
 	@touch $@
 
 endif
